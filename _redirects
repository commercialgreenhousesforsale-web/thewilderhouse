# Clean URL redirects for Cloudflare Pages
# Redirect .html extensions to clean URLs

/savannah-ghost-tour.html /savannah-ghost-tour 301
/index.html / 301

# Catch-all for .html files
/*.html /:splat 301

# Service worker must be at root
/service-worker.js /service-worker.js 200
